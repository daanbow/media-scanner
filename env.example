# Copy this file to `.env` and replace with your real key.
# The app will read TMDB_API_KEY from the environment.
# If you use VS Code, it can auto-load .env (default: ${workspaceFolder}/.env).
# Otherwise, install `python-dotenv` and add the following to your script:
#   from dotenv import load_dotenv
#   load_dotenv()

# Copy to .env and fill your real key
TMDB_API_KEY=REPLACE_WITH_YOUR_TMDB_KEY
